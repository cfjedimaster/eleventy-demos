---
layout: main 
title: On this Day Test 
---

<h2>List of Posts</h2>

{% for post in collections.posts reversed %}

	<a href="{{ post.url }}">{{ post.data.title }}</a> ({{ post.date | date: "%B %d, %Y %l:%M%P" }})<br/>

{% endfor %}